{
  "name": "mindmeld",
  "version": "0.6",
  "description": "A web-based mind mapping tool",
  "main": "src/js/app.js",
  "type": "module",
  "scripts": {
    "start": "live-server src --port=8080 --no-css-inject --cors",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write 'src/**/*.{js,html,css}'",
    "format:check": "prettier --check 'src/**/*.{js,html,css}'"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@eslint/js": "^8.0.0",
    "eslint": "^8.0.0",
    "eslint-plugin-prettier": "^5.2.1",
    "live-server": "^1.2.2",
    "prettier": "^3.3.3"
  },
  "dependencies": {
    "@playwright/test": "^1.46.0"
  }
}
